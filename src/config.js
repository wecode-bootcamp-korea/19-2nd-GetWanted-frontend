const IP = '3.142.122.115:8000';

export const GET_LOGIN_API = `http://${IP}/signin`;
export const GET_APPLYLIST_API = `http://${IP}/applylist`;
export const GET_JOBLIST_API = `http://${IP}/notifications`;
export const GET_LIKE_API = `http://${IP}/notifications/like`;

export const EMAIL_API = `http://${IP}/users/email`;
export const LOGIN_API = `http://${IP}/users/signin`;
export const SIGNUP_API = `http://${IP}/users/signup`;
export const PASS_RESET_API = `http://${IP}/users/reset`;
export const KAKAO_LOGIN_API_URL = `http://${IP}/users/kakao`;

export const STORAGE_FORMS_API = `http://${IP}/resumes`;
export const STORAGE_FILES_API = `http://${IP}/resumes/files`;
export const STORAGE_LIST_API = `http://${IP}/resumes/lists`;
export const RESUME_API = `http://${IP}/resumes`;

export const JOB_DETAILS_API = `http://${IP}/notifications`;
export const JOB_DETAILS_USER_API = `http://${IP}/apply`;
export const JOB_DETAILS_RESUME_API = `http://${IP}/resumes/lists`;
